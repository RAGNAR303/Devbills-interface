import Button from "../components/Button";

const Home = () => {
  return (
    <div>
      <h2>Home</h2>
      <Button  variant="outline" isLoanding>Clique em min</Button>
    </div>
  );
};

export default Home;
